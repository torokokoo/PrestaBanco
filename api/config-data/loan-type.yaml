server:
  port: 8093
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pb-loan-type
    username: postgres
    password: root
  eureka:
    instance:
      preferIpAddress: true
    client:
      fetch-registry: true
      register-with-eureka: true
      service-url:
        defaultZone: http://localhost:8761/eureka/
